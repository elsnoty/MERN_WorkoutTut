{
    "version": 2,
    "builds": [
        {
            "src": "/server.js/api",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ]
}